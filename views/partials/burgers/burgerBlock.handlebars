<li>
    {{#unless devoured}}
    {{burger_name}}
    <button type="button" data-id="{{id}}" class="devourButtons">Devour It!</button>
    {{else}}
    <span class="devoured">{{burger_name}}</span>
    {{/unless}}
</li>